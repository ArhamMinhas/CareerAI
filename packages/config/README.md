# packages/config

Shared configuration: ESLint config, TypeScript base config, Tailwind preset (design tokens),
Prettier config. Consumed by `apps/web` and `packages/ui` to avoid duplicated tooling config.
Built starting Phase 1.
