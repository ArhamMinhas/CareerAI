# packages/ui

Shared React component library consumed by `apps/web`: design-token-driven primitives
(Button, Input, Card, Modal, Tabs, Chart wrappers, DataTable, EmptyState, ...). Built
incrementally starting Phase 2. See [docs/UI_ARCHITECTURE.md](../../docs/UI_ARCHITECTURE.md).
