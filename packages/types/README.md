# packages/types

Shared TypeScript types consumed by `apps/web`: domain types mirroring backend Pydantic
schemas (Resume, Skill, Job, Interview, ...) plus types generated from the FastAPI OpenAPI
schema. Keeps frontend and backend contracts in sync. Built starting Phase 1.
