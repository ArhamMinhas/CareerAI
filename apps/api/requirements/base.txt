fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30
alembic>=1.14
python-multipart>=0.0.12
pyjwt[crypto]>=2.9
celery[redis]>=5.4
redis>=5.2
httpx>=0.27
pypdf>=5.0
python-docx>=1.1
openai>=1.50
google-genai>=2.0
pgvector>=0.3

# Trained-model inference only (docs/ML_PIPELINE.md §3, §6 — Phase 8) — lean, no pandas/jupyter/
# matplotlib here; those are ml/requirements.txt's training-only concern (see that file's
# docstring for why the two are deliberately kept separate).
scikit-learn>=1.5
xgboost>=2.1
joblib>=1.4
numpy>=1.26
