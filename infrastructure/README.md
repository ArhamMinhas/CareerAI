# infrastructure/

```
docker/         Dockerfiles for web, api, worker + docker-compose for local dev
deployment/     environment-specific deployment configs (staging, production)
```

See [docs/DEPLOYMENT.md](../docs/DEPLOYMENT.md). Built starting Phase 1 (local Docker Compose),
hardened in Phase 16 (production deployment).
